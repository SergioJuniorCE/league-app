{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir=out --target=node",
    "start": "bun run out/index.js",
    "lint": "bun x tsc -p tsconfig.json"
  },
  "dependencies": {
    "elysia": "^1.2.25"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.2.2"
  }
}
