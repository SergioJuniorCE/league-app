# Riot Games API key used by the Electron main process to fetch summoner data.
# Get a development key from https://developer.riotgames.com/ (expires every 24h).
# The renderer never sees this value — it's read in electron/main.ts via dotenv.
RIOT_API_KEY=RGAPI-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
